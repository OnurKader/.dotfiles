#!/usr/bin/env sh

[ -z $1 ] && cargo init && exit 0

cargo new "$1" && cd "$1" && cp ~/.config/scripts/rust-gitignore ./.gitignore
cp ~/.config/scripts/MIT ./LICENSE

git add LICENSE Cargo.toml .gitignore src/

