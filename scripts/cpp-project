#!/bin/bash

git init
cp ~/scripts/cpp-gitignore .gitignore
mkdir src bin include

cp ~/scripts/meson.build .

echo "# <++>" >> README.md

git add .gitignore src README.md meson.build
git commit -am "Initial Commit"

